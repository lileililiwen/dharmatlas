services:
  host:
    image: dharmatlas:${IMAGE_TAG:-staging}
    user: app
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M
    environment:
      ASPNETCORE_URLS: http://+:8080
      DHARMATLAS_OTLP_ENDPOINT: ${DHARMATLAS_OTLP_ENDPOINT:-}
      DHARMATLAS_SENTRY_DSN: ${DHARMATLAS_SENTRY_DSN:-}
    env_file:
      - staging.env
    # Staging uses the externally managed database referenced by staging.env;
    # there is intentionally no local postgres service here.
