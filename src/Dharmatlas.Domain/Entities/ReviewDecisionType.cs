namespace Dharmatlas.Domain.Entities;

/// <summary>The disposition a reviewer applies to a pending submission.</summary>
public enum ReviewDecisionType
{
    Approve,
    RequestChanges,
    Reject
}
